<%= simple_form_for user_product do |f| %>
  <%= select_tag("categories", options_from_collection_for_select(Category.all, "id", "name", params[:category_name]))%>
  <%= hidden_field_tag :product_name, @product_name %>
  <%= f.input :purchase_date %>
  <%= f.input :place %>
  <%= f.input :peremption_date %>
  <%= f.button :submit %>
<% end %>


<%#= f.collection_select(:category_id, Category.all, :id, :name) %>

