<%= simple_form_for user_product do |f| %>

  <%= hidden_field_tag :product_name, @product_name %>

  <!-- search pour catégories -->

  <%= select_tag("categories", grouped_options_for_select(Category.order), class: "search-form-new") %>

  <%# collection_select(:categories, :category_id, Category.order.values.map { |a| a }.flatten.uniq, :id, :name, {}, class: "search-form-new", id: "testing") %>

  <%= f.input :place %>


<!--   <select id="testing">
    <%# Category.order.each do |key, value| %>
      <optgroup label='<%#= key %>'>
        <%# value.each do |val| %>
          <option><%#= val.name.capitalize %></option>
        <%# end %>
      </optgroup>
    <%# end %>
  </select> -->


  <%= f.input :purchase_date %>


  <%= f.input :peremption_date %>
  <%= f.button :submit %>
<% end %>


    <!-- grouper les categories par subcategory -->



    <!-- pour chaque sous category -->

    <!-- pour chaque sous category -->

  <%#= collection_select(:category, :category_id, Category.all, :id, :name, {}, class: "search-form-new") %>

<%#= f.collection_select(:category_id, Category.all, :id, :name) %>


